<div class="center">
  <h3>新規登録</h3>

  <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
    <%= render "devise/shared/error_messages", resource: resource %>

    <div class="field form-group col-lg-4 col-lg-offset-4">
      <%= f.label :名前 %><br />
      <%= f.text_field :name, autofocus: true, placeholder: '名前 *', autocomplete: "name", class: "form-control" %>
    </div>


    <div class="field form-group col-lg-4 col-lg-offset-4">
      <%= f.label :メールアドレス %><br />
      <%= f.email_field :email, autofocus: true, placeholder: 'メールアドレス *', autocomplete: "email", class: "form-control" %>
    </div>

    <div class="field form-group col-lg-4 col-lg-offset-4">
      <%= f.label :パスワード %>
      <% if @minimum_password_length %>
      <em>(<%= @minimum_password_length %> 文字以上)</em>
      <% end %><br />
      <%= f.password_field :パスワード, placeholder: 'パスワード *', autocomplete: "new-password", class: "form-control" %>
    </div>

    <div class="field form-group col-lg-4 col-lg-offset-4">
      <%= f.password_field :password_confirmation, placeholder: 'パスワード確認 *', autocomplete: "new-password", class: "form-control" %>
    </div>

      <%= f.submit "登録", class: "btn btn-info col-lg-4 col-lg-offset-4 col-xs-12" %>

  <% end %>
</div>

  <div class="center form-group col-lg-6 col-lg-offset-3">
  <%= render "users/shared/links" %>
  </div>